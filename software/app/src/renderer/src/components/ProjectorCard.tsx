import { Button, Card } from 'antd'

function ProjectorCard(): React.JSX.Element {
  return (
    <Card size="small" style={{ height: '100%' }}>
      <Button type="primary">Projector</Button>
    </Card>
  )
}

export default ProjectorCard
